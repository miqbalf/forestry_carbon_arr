# Windows / ArcGIS Pro compatible requirements.
#
# This file is intended for use inside ArcGIS Pro's Python environment on Windows,
# where `arcgis` often pins `pyarrow` to < 21 and requires `pywin32`.
#
# If you're not using ArcGIS Pro / the ArcGIS Python API, prefer `requirements.txt`.

# Core dependencies (required)
numpy>=1.24.0
pandas>=2.0.0
geopandas>=0.14.0
shapely>=2.0.0
pyproj>=3.6.0
fiona>=1.9.0
rasterio>=1.3.0

# STAC and time series processing (required)
xarray>=2023.1.0
rioxarray>=0.15.0
xee>=0.0.8
scikit-learn>=1.3.0
catboost>=1.2.2
shap>=0.44.1
matplotlib>=3.7.0
seaborn>=0.12.2
tqdm>=4.66.0

# Configuration and utilities
pyyaml>=6.0.0
python-dotenv>=1.0.0
click>=8.1.0

# Planet API integration
planet>=3.4.0
gcsfs>=2023.1.0
folium>=0.15.0

# ArcGIS Pro compatibility pins (ArcGIS Python API 2.4.x)
pyarrow>=17,<21
pywin32>=223

# Optional dependencies (install with extras)
# GEE: pip install forestry_carbon_arr[gee]
